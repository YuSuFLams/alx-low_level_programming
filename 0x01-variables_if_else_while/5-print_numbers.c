#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Get a random number and check its last digit, compare it with 5
 * Return: 0
 */
int	main(void)
{
	int	test;

	test = 47;
	while (test++ < 57)
		putchar(test);
	putchar(10);
	return (0);
}
